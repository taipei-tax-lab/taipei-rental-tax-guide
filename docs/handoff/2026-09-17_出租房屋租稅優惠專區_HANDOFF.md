# 出租房屋租稅優惠專區 Handoff

## 1. 專案目前狀態

- **正式版本**：已完成公益出租人認定流程圖官方 PDF 連結新增與 0915 PDF 完整交付，並成功合併發布至 `main`
- **Public landlord flow PDF production release merge commit**：`099853778369feee17231ca79869ed877fe46c79` (`0998537`)
- **main 分支狀態**：`main` 已包含本次 release（PR #5）及後續 Handoff 文件更新
- **PR #5**：已 merged（Standard Merge commit 方式合併）
- **GitHub Pages**：已部署成功（狀態 `built`，deployment commit `0998537`）
- **Production smoke test**：正式站 100% 通過（含 5 種視窗寬度、3 種 Zoom、互動功能、政策抽查、PDF 連結與無水平溢出）
- **目前版本完成範圍**：
  - V2 Refinement UX（閱讀動線優化、桌機 inline-plan、手機雙方案並排比較）
  - Dialogflow Messenger viewport refinement（縮放與短視窗定位、輸入框防遮蔽）
  - 財產稅科 0915 PDF 政策內容校稿（四方案條件、稅率說明、官方入口更新）
  - 快速判斷推薦結果 `.v2-full-condition` hook regression 修復
  - 全站排版斷行自然化（`.v2-other-taxes > span`、`.v2-unit` inline、短標題/方案名稱 `text-wrap: wrap`）
  - 0915 PDF 全頁面回歸 Audit 確認無政策落差與無多餘非專案檔案
  - 認定公益出租人流程圖官方 PDF 連結正式補上（直連 www-ws.gov.taipei 官方來源）

## 2. Release 狀態

- **最新正式發布 PR**：#5（補上公益出租人認定流程圖官方 PDF 連結）
- **PR 狀態**：已 merged
- **Public landlord flow PDF production release merge commit**：`099853778369feee17231ca79869ed877fe46c79`
- **main 分支**：已包含本次 release 及後續 Handoff 文件更新
- **GitHub Pages**：已部署完成（網址：`https://taipei-tax-lab.github.io/taipei-rental-tax-guide/`，deployment commit: `0998537`）
- **Production smoke test**：已全數通過
  - Desktop 1440px / 1280px / 1024px 正常
  - Tablet 768px 正常
  - Mobile 390px 正常
  - Zoom 100% / 125% / 150% 正常
  - 公益出租人詳細頁 sourceLinks 正確包含「認定公益出租人流程圖（PDF） ↗」
  - Production DOM href 直連臺北市政府官方 PDF，無 chrome-extension:// wrapper
  - 官方 PDF 連線驗證：HTTP 200 / application/pdf / %PDF-1.5 / 149,197 bytes
  - `.v2-other-taxes .v2-unit` computed display = `inline`（地價稅 10‰至55‰ 不再被強制拆成零碎 block line）
  - `.v2-plan-name` computed text-wrap = `wrap`（短方案名稱不再因 balance 被提早強迫換行）
  - 快速判斷正常（推薦結果與條件文案正常渲染）
  - 手機雙方案比較正常（並排切換流暢）
  - Messenger 正常（開啟、短/矮視窗高度自適應、關閉正常）
  - horizontal overflow = 0
  - pageerror = 0
  - console.error = 0

> **註記**：GitHub repository 目前未建立 CI workflow，上述 browser/runtime 驗證主要來自 AntiGravity 本機與 production smoke test。

## 3. 版本演進

```text
990a093  V2
   │
   └─ 26944218  V2 Refinement
        │
        └─ 8e45fbe5  Messenger viewport 修正
             │
             └─ acb15d3  0915 PDF 修正
                  │
                  └─ 948e64e  guide condition hook fix
                       │
                       └─ 07d90b6  release handoff
                            │
                            └─ 0933917  Merge PR #3 → main
                                 │
                                 ├─ 818c95a  typography wrap fix
                                 ├─ dbcdae5  typography audit handoff
                                 │
                                 └─ a7a4da8  Merge PR #4 → main (Typography release)
                                      │
                                      ├─ 4dfe276  add official flow PDF link
                                      ├─ d0fee50  resolve flowchart handoff item
                                      │
                                      └─ 0998537  Merge PR #5 → main (Flow PDF release)
```

**版本背景說明**：
V2 Refinement 先前已部署至 GitHub Pages 供業務單位檢視，財產稅科承辦人員實際列印並進行文字批註校核的依據是 Refinement 畫面，而非初始的 V2 版本。因此「出租專區修正內容0915-1.pdf」之校稿需求，核心即為「以 V2 Refinement 為基準進行最新政策文字與官方連結校正」。

## 4. 架構

本專案為靜態網站架構，所有主要頁面由建置腳本統一編譯輸出：

- `site/content.json`：單一政策資料來源（四方案文案、稅目、適用條件、辦理順序、注意事項、外部連結、房客服務與常見問答）。
- `site/template.html`：全站 HTML 骨架模板，包含佔位符（Placeholders）。
- `scripts/build.mjs`：建置腳本，負責讀取資料、組裝 HTML、計算資源雜湊並產出 `index.html`。
- `assets/css/guide-v2.css`：V2 / Refinement 全站樣式表（含卡片 subgrid、雙方案比較、響應式斷點）。
- `assets/js/guide-rules.js`：快速判斷決策樹規則引擎（純邏輯，不依賴 DOM）。
- `assets/js/guide-ui.js`：使用者互動控制（快速判斷問答渲染、推薦卡片組裝、雙方案切換、桌機 inline-plan 展開）。
- `site/messenger.html`、`assets/css/messenger.css`、`assets/js/messenger-ui.js`：Dialogflow CX 小幫手元件、樣式與視窗適應邏輯。
- `index.html`：建置輸出產物，禁止直接手動修改，任何變更均須透過 `scripts/build.mjs` 重建。

## 5. 本次 0915 修正的重要內容

- **方案文字校對**：更新四種方案在首頁與詳細頁的說明，公益出租人與社宅之首頁 `cardDescription` 與詳細頁 `suitable` 保持細緻區隔。
- **主動辦理與主動申報規範**：
  - 公益出租人與社會住宅包租代管：地方稅由稅捐處依都發局造冊資料主動辦理；綜所稅依規定向國稅局申報。
  - 一般出租與個人包租代管：地方稅須由屋主自行於法定時限前向稅捐處提出申請（房屋稅 3/22 前、地價稅 9/22 前）。
- **完整適用條件排版移轉**：由原先頁尾收合的 `<details>` 移至詳細頁「適合誰？」正下方，以有序清單常駐展開呈現。
- **Next Step 資料層驅動**：詳細頁下一步區塊改由 `site/content.json` 直接控制標題、指引與按鈕文案。
- **FAQ 擴充**：支援多按鈕結構（例如租金補貼題同時提供公益出租人與四方案比較連結；租金標準題提供試算與當地標準法規連結）。
- **房客服務重整**：房客常見需求擴充為 12 項，分組改為 5 / 3 / 4，並補足「請依各機關最新公告為準。」附註。
- **官方窗口常駐**：將「官方申辦與聯絡方式」由折疊 `<details>` 改為常駐顯示的 `<section>`，並納入國稅局服務據點。
- **詳細頁日期劃除**：詳細頁不再顯示核對與更新日期（相關 metadata 仍保留於 JSON 資料層以供維護）。
- **完整保留 Refinement UX**：完整保留手機雙方案選單並排比較、數字單位 `keepUnits()` 標記、Messenger 版本快取雜湊等。

## 7. 最近修復的 Regression（歷程）

- **現象**：快速判斷走到推薦結果時，`guide-ui.js` 會嘗試以 `document.querySelector('#plan-' + id + ' .v2-full-condition').textContent` 取得主要適用條件組裝「仍須確認：……」文案。0915 改版將 conditions 改為列表後移除了該 class，導致執行時期出現 `null.textContent` 例外。
- **修復（Commit `948e64e`）**：在 `scripts/build.mjs` 中，於 `plan.conditions` 的第一個項目保留 `class="v2-full-condition"`。
- **成果**：無須改動 `guide-ui.js`，畫面樣式無任何副作用，推薦流程能精準讀取首要條件文字，並已於 `tests/build.test.mjs` 納入迴歸測試防線。

## 8. Typography / line wrapping regression（正式修復與 Production 驗證）

本項目修復正式上線後於真實瀏覽器排版審查時發現的斷行與文字包裹問題，現已正式發布並通過 Production 驗證：

- **問題原因**：
  1. `.v2-other-taxes span { display:block; }` 選擇器範圍過廣，誤命中內層由 `keepUnits()` 產生的 `<span class="v2-unit">`，導致一般出租卡的「10‰」與「55‰」等數字單位各自被轉為獨立 block，在空間充裕時被拆成「地價稅：一般用地稅率」、「10‰」、「至」、「55‰」多行。
  2. `.v2-site :is(..., .v2-plan-name) { text-wrap: balance; }` 的平衡斷行演算法在中文排版下，對短方案名稱（如「個人租賃住宅包租代管（法人不適用）」）造成提前強迫換行，破壞了「能放滿目前行就放滿、空間不足才自然折行」之原則。
- **修正方式**：
  1. 將選擇器範圍限縮為 direct child：`.v2-other-taxes > span { display:block; }`，並於 `.v2-unit` 明確指定 `display: inline; white-space: nowrap;`，確保「地價稅」與「綜合所得稅」維持分行 block，而內部數字單位保持 inline。
  2. 將全站短標題、按鈕、方案名稱的 `text-wrap: balance` 改為 `text-wrap: wrap` 自然換行，移除提前平衡換行行為；遵循「放不下才自然斷，不粗暴使用全域 nowrap」之原則。
- **Regression Test 防線**：
  1. 單元測試（`tests/build.test.mjs`）：加入 CSS 靜態規則驗證，防範 `.v2-other-taxes span` 誤用與 `.v2-plan-name` 誤加 balance。
  2. 整合測試（`scripts/browser-check.cjs`）：加入 `.v2-other-taxes .v2-unit` computed display 為 `inline` 及 `.v2-plan-name` computed textWrap 不為 `balance` 之檢查。
  3. 跨裝置端到端測試（Playwright Chrome）：涵蓋 Desktop 1440/1280/1024、Tablet 768、Mobile 390 及 100%/125%/150% 縮放，全數通過且 horizontal overflow = 0、pageerror = 0、console.error = 0。
- **上線與驗證狀態**：已於 PR #4（Merge commit `a7a4da8f05da181fd5b50f71fa57b6f7edd0b721`）正式合入 `main`，GitHub Pages 自動部署成功（狀態 `built`），且經真實 Chrome 瀏覽器 Production Smoke Test 100% 通過。

## 9. 0915 PDF 完整內容再 Audit 結果

針對財產稅科「出租專區修正內容0915-1.pdf」Page 1～9 進行逐頁重新對照與自動化驗證：
- **Page 1～2 & 9（一般出租）**：適合情境、1.5%/2%/2.4%、租金標準、10‰至55‰、所得稅申報規定均 100% 正確。
- **Page 1～2 & 8（公益出租人）**：租金補貼資格、地價稅 2‰、所得稅 15,000 元免稅額與 43% 扣除規定、地方稅造冊主動辦理均 100% 正確。
- **Page 1～2 & 7（社會住宅包租代管）**：透過租屋服務事業出租合法住宅、地價稅減徵 80%、所得稅 15,000 元免稅額與 60% 扣除、Next Step 合作業者與 B5020 連結均 100% 正確。
- **Page 1～2 & 6（個人租賃住宅包租代管）**：（法人不適用）標示、房屋稅/地價稅減徵 40%（每屋上限 1 萬元）、所得稅 6,000 元/53%/43% 級距、3/22 與 9/22 申請時限均 100% 正確。
- **Page 3（FAQ）**：租金補貼導流、包租代管差異、租金標準與 114 年標準法規連結（GL011704）、主動辦理與自行申報區隔均 100% 正確。
- **Page 3（官方窗口）**：稅捐處與國稅局聯絡資訊及服務據點已改為常駐顯示（非折疊 details），100% 符合要求。
- **Page 4（房客服務）**：5 / 3 / 4 群組分組、安心樂租網、國家住都中心、地政局友善屋源連結及「請依各機關最新公告為準。」附註均 100% 正確。
- **Page 5（四方案比較）**：四方案情境、管理方式、主要門檻、三稅與第一步均 100% 正確。
- **結論**：Production 政策內容與 PDF 完全一致，無任何漏項或政策 mismatch。

## 10. 歷史 unresolved item 與目前狀態

- **「認定公益出租人流程圖（PDF）」**：**已解決（Resolved）並經 Production 驗證上線**
  - **歷史背景**：此前因無法確認官方公開 URL，依嚴格不造假原則暫不加入；
  - **解決歷程（2026-09-17）**：已於臺北市稅捐稽徵處官方「出租房屋租稅優惠專區－公益出租人」頁面確認「認定流程」之官方下載連結（`https://www-ws.gov.taipei/Download.ashx?u=LzAwMS9VcGxvYWQvMzM2L2NrZmlsZS8wODlkZDg0Yi03NjJiLTQ3ZTYtYjUwMC1hNWVmN2VlODdiMWQucGRm&n=UFBU56ysM%2bW8tV%2flhaznm4rlh7rnp5%2fkurroqo3lrprmtYHnqItQREYucGRm&icon=.pdf`），經連線驗證為有效的官方 PDF（HTTP 200, Content-Type: application/pdf, %PDF-1.5, 149,197 bytes）。
  - **實作方式**：已納入公益出租人詳細頁之 `public.sourceLinks`（官方資訊與申辦入口），直連臺北市政府現行官方來源，不另存本地副本、無 `chrome-extension://` wrapper，並於 build script 補強 sourceLinks HTTPS 驗證。
  - **上線與驗證**：已於 PR #5（Merge commit `099853778369feee17231ca79869ed877fe46c79`）正式合入 `main`，GitHub Pages 部署成功（狀態 `built`），且經真實 Chrome 瀏覽器 Production Smoke Test 100% 通過。
- **目前已知 unresolved item**：**無（0）**（0915 PDF Page 1～9 全部修改需求已正式 100% 達成）

## 11. Branch 定位

- `main`：現行正式 production 版本（包含 PR #3、PR #4、PR #5 及最新 post-release 狀態）。
- `fix/favicon-and-messenger-layout`：本次 favicon 換用官方 mark、Messenger 使用提醒防截斷、以及桌機 assistant panel 與 chat window 零縫隙接合修正分支。
- `fix/add-public-landlord-flow-pdf`：已完成並 merge 的公益出租人流程圖 PDF 發布分支，保留作歷史 reference。
- `fix/typography-wrap-0915-audit`：已完成並 merge 的 typography 修正發布分支，保留作歷史 reference。
- `feature/refinement-property-tax-revision-0915`：已完成並 merge 的 0915 政策修訂發布分支，保留作歷史 reference。
- `codex/rental-guide-v2-refinement`：Refinement 歷史 reference。
- `feature/property-tax-revision-0915`：舊政策校稿 reference branch，不再 merge。

## 12. Favicon 與 Messenger Layout UI 修正（2026-09-17 本輪交付）

本輪針對正式站與測試站審查提出的三項前端 UI / Messenger 問題進行專項修復，完全未觸動政策資料（`content.json`）及 Dialogflow CX 後端設定：

### 1. Browser Favicon 換用臺北市稅捐稽徵處官方 Mark
- **問題**：原 `favicon.svg` 為四個藍色方塊之 generic icon，無法展現官方機關識別。
- **作法**：從既有品牌資產 `assets/images/tpctax-logo.png` 左側乾淨裁切出官方獨立 mark（TRTS 藍金雙色環徽），輸出為 128×128 透明背景專用圖檔 `assets/images/tpctax-mark.png`，保留原比例、不拉伸、不改色、不含「臺北市稅捐稽徵處」中文字樣，確保在 16px/32px browser tab 下仍清晰可辨。
- **模板與建置**：`site/template.html` 替換為 `<link rel="icon" href="./assets/images/tpctax-mark.png" type="image/png">`，經 `build.mjs` 重建同步更新 `index.html`；header 仍維持使用完整 `tpctax-logo.png`。

### 2. Messenger 中間使用提醒（Notice）永遠完整顯示
- **問題根因**：原 `.rental-input-extras` 宣告 `flex: 0 1 auto; min-height: 0; max-height: 35%; overflow-y: auto;`，當對話內容增多或視窗高度較矮時，整區會被 flex-shrink 壓縮產生獨立捲軸；且 `updateInputExtras` 在 compact viewport（height < 400px）會將 notice 移入 `<details>` 折疊。
- **修正作法**：
  1. `.rental-input-extras` 改為 `flex: none; box-sizing: border-box; overflow: visible;`，其內部 `p` 宣告 `flex: none; overflow: visible;`，成為固定不可被壓縮的常駐區塊。
  2. 移除將 notice 移入 `<details>` 的程式碼，提醒文字永遠置於 `<details>` 外部，維持兩行完整顯示。
  3. 若使用者展開「熱門問題」，僅限制 `.messenger-topic-list` 本身之 `max-height: 180px; overflow-y: auto;`（矮視窗 120px），不干擾提醒文字。
- **驗證**：在無訊息、短回答、長回答（600字）、多輪回答及矮視窗下，`notice.scrollHeight <= notice.clientHeight + 1` 均成立，無文字裁切、無自身 scrollbar。

### 3. Assistant Panel 與 Chat Window 零縫隙接合
- **問題根因**：原程式以 `elements.assistantPanel.style.right = (window.innerWidth - rect.left - overlap) + 'px'` 定位，但在 Windows Chrome / Edge 下，當主頁面有內容垂直捲軸時，`window.innerWidth` 包含系統捲軸寬度（17px），而 `position: fixed` 的 right 是相對於 `clientWidth`（不含捲軸），導致 panel 右側與 chat 左側產生剛好 16～17px 的懸空縫隙；此外，chat 打開時具有 300ms 的 CSS transition，單一 frame 的 RAF 無法捕捉穩定後的 chatRect。
- **修正作法**：
  1. `.assistant-panel` 明確加入 `box-sizing: border-box;`。
  2. 定位改採直接 viewport `left` 基準：`panel.style.left = (rect.left - panelWidth + overlap) + 'px'`、`panel.style.right = 'auto'`、`panel.style.top = rect.top + 'px'`、`panel.style.bottom = 'auto'`、`panel.style.height = rect.height + 'px'`，徹底消除捲軸寬度造成的差值。
  3. `openChat` 觸發時加入雙重 RAF 及 150ms / 350ms 檢查點，並掛載 `ResizeObserver` 與 `transitionend` 監聽。
- **驗證**：在 1440×900、1280×800、1024×768 及 100%、125%、150% 縮放實測中，`abs(gap) <= 1px`、`top difference <= 1px`、`bottom difference <= 1px` 均 100% 達成，外側維持 22px 圓角、接合處平直無雙框線與中間陰影縫。

### 4. 測試防線與驗證結果
- **單元測試**（`agy-node.cmd --test tests/build.test.mjs tests/guide-rules.test.mjs`）：14 項全數 PASS（含新增的 favicon 檔案與參照檢查、Messenger notice 不移入 details 與 panel left 對齊規則檢查）。
- **建置檢查**（`agy-node.cmd scripts/build.mjs --check`）：PASS，產生之 `index.html` 與原始碼完全一致。
- **瀏覽器端到端回歸**（`scripts/messenger-regression.py`，Python Playwright + Chrome）：12 組 Viewport/Zoom 矩陣、4 種訊息長度、矮視窗、草稿保留、狀態切換全數 PASS。
- **水平溢出**：所有 Viewport 與 Zoom 下 `horizontalOverflow = 0`。
- **主控台錯誤**：`pageerror = 0`、`console.error = 0`。

## 13. 下一步

1. 將分支 `fix/favicon-and-messenger-layout` push 至 remote。
2. 提交 GitHub Code Review，等待進一步指示。
3. 後續修改仍應維持：
   - `content.json` 作政策資料來源
   - `index.html` 由 build 產生
   - 不直接手動修改 generated HTML
   - 修改後執行 build / test / browser regression
