{
  "name": "taipei-rental-tax-guide",
  "version": "2.0.0",
  "private": true,
  "description": "臺北市出租房屋租稅優惠專區：可重建的 GitHub Pages 靜態網站",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "node scripts/serve.mjs"
  },
  "engines": { "node": ">=20" }
}
